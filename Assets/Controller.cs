using System.Collections;
using System.Collections.Generic;
using Menu;
using Unity.VisualScripting;
using UnityEngine;

public class Controller : MonoBehaviour
{
    
    void Start()
    {
   //    PauseMenuController.OnPauseMenuToggled += opened =>
   //    {
   //        if (!opened)
   //        {
   //            return;
   //        }

   //        var test = GameObject.Find("test");
   //        var content = GameObject.Find("PauseContent");
   //        test.transform.SetParent(content.transform, new Vector3(0, 0, -0.1f));

   //    };

    }
    
    // Update is called once per frame
    void Update()
    {
        
    }
}
